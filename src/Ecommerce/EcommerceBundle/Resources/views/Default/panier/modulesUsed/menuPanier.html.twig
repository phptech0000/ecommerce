<div class="well">
    <ul class="nav nav-list">
        <li class="nav-header">Panier</li>
        <li>
            <a href="{{ path('panier') }}">
                {% if nbArticles == 0 %}
                    Aucun article dans votre panier
                {% elseif nbArticles == 1 %}
                    1 article dans votre panier
                 {% else %}
                    {{ nbArticles }} articles dans votre panier
                {% endif %}
            </a>
        </li>
    </ul>
</div>